{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "express",
  "installCommand": "cd .. && npm install --include=dev",
  "buildCommand": "cd .. && npx prisma generate --schema=prisma/schema.prisma && npx nx build @org/chat-service --configuration=production --skip-nx-cache",
  "functions": {
    "src/index.js": {
      "includeFiles": "dist/**",
      "maxDuration": 60
    }
  }
}